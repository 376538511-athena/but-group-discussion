# 2026-04-05 工作记录

## 今日目标

- 阅读当前项目部署说明，确认上线方式
- 准备将现有前端改动推送到 GitHub，并通过 Cloudflare Pages 上线
- 确认 Supabase 已补充 SQL 后的上线要点与回滚方式
- 新增“笔记”模块并完成本地可测试版本

## 已确认的部署结论

- 当前项目采用 `Cloudflare Pages + Supabase` 方案，不走旧的 Docker/Nginx 服务器部署
- 前端构建已多次本地验证通过
- 之前本地已有一次提交：
  - commit: `dc202f2`
  - message: `feat: add weekly task workflow and member features`
- GitHub 推送时本机缺少可用的 HTTPS 凭据，因此未由助手代推

## 今日完成的功能改动

### 1. 笔记模块

- 新增侧边栏菜单：`上传笔记`
- 新增页面：`/notes/upload`
- 上传笔记支持：
  - 笔记标题
  - 笔记时间
  - 笔记摘要
  - `1` 个 PDF，或最多 `9` 张 JPG/PNG
  - 不允许 PDF 和图片混传

### 2. 笔记展示

- 个人资料页新增 `我的笔记` tab
- BUT 成员页进入的成员资料卡中新增 `我的笔记` tab
- 成员资料卡中隐藏 `我的收藏`
- 图片笔记支持预览
- PDF 笔记支持打开文件查看

### 3. 笔记删除

- 在“我的笔记”列表中新增删除入口
- 删除入口最终样式为仅显示垃圾桶 icon，不显示“删除”文字
- 删除时会同时删除：
  - `notes` 表中的记录
  - Supabase Storage 中对应文件
- 成员资料页中的笔记列表保持只读，不显示删除按钮

### 4. UI 微调

- 调整“我的笔记”tab 的图标与文字间距
- 个人资料页和成员资料页两处均已统一

## 新增/修改的主要文件

- `client/src/pages/UploadNotePage.tsx`
- `client/src/api/notes.ts`
- `client/src/components/notes/NoteList.tsx`
- `client/src/pages/ProfilePage.tsx`
- `client/src/pages/MemberProfilePage.tsx`
- `client/src/components/layout/AppLayout.tsx`
- `client/src/App.tsx`
- `client/src/lib/supabase.ts`
- `client/src/types/note.ts`
- `docs/migration-v4.sql`
- `docs/migration-all-in-one.sql`
- `docs/supabase-schema.sql`
- `docs/cloudflare-pages-supabase.md`
- `client/.env.example`

## Supabase 相关记录

### 数据库

- 已新增 `notes` 模块对应迁移文件：`docs/migration-v4.sql`
- `notes` 表包含：
  - 标题
  - 笔记时间
  - 摘要
  - 附件列表（jsonb）
  - 上传者

### Storage

- 需要创建 `notes` bucket
- 该 bucket 不应设为 public
- 当前建议配置：
  - `INSERT` policy 给 `authenticated`
  - `SELECT` policy 给 `authenticated`

### 用户确认过的操作

- 用户曾将 `notes` bucket 设为 public，后续建议改回 private
- 已说明可在 Supabase Storage 的 bucket settings 中关闭 public

## Cloudflare 相关记录

- 需要在 Cloudflare Pages 中新增环境变量：
  - `VITE_SUPABASE_NOTES_BUCKET=notes`
- 该变量应至少在 `Production` 和 `Preview` 中都配置
- 新增环境变量后需要重新部署，变量才会生效

## GitHub / Cloudflare / Supabase 协作者说明

- 已向用户说明三方平台的协作者添加流程：
  - GitHub：仓库协作者
  - Cloudflare：成员 + policy
  - Supabase：组织/团队成员
- 对 Cloudflare 权限已建议：
  - `Workers Platform Admin` 适合 Pages 运维与修改
  - 如需修改 DNS，可再加 `Domain DNS`
- 不建议直接授予 `Super Administrator`

## 本地测试记录

- 本地前端开发服务已成功启动过
- 本地访问地址：
  - `http://127.0.0.1:4173/`
- 本地构建验证命令：
  - `npm run build --workspace=client`
- 多次构建均成功通过

## 今日对话中的关键决定

- “需求整理文档”不作为当前上线的约束，以当前代码版本为准
- 当前优先目标是先完成本地测试，再由用户手动推 GitHub 上线
- 成员资料卡中保留“我的笔记”，隐藏“我的收藏”
- 删除笔记入口最终采用纯垃圾桶图标

## 需要注意的事项

- `client/tsconfig.tsbuildinfo` 是构建缓存，不建议提交
- 若后续上线“笔记”模块，需先完成：
  - Supabase `notes` 表迁移
  - `notes` bucket 创建
  - storage policy 配置
  - Cloudflare Pages 环境变量补充

## 记录时间

- 保存时间：2026-04-05 23:36:46 CST
